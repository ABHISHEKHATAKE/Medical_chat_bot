# NOTE: Vite embeds these at server start. After editing this file,
# restart the frontend (Ctrl+C, then `npm run dev`) or the change is ignored.
# Without a real pk_ key the app falls back to the DEV login form.
VITE_API_URL=http://localhost:5000
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
